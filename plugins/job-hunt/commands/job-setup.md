---
description: One-time setup — create your Job Hunt workspace, profile, and master CV from your existing CVs.
---

Run the `job-setup` skill to guide me through one-time setup: create my workspace,
build my `profile.json` (contact, target locations, platforms, work-auth setting,
apply preferences), and import my existing CV PDFs into a validated `cv_master.json`.
