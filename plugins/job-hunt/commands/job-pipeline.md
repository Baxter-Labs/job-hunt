---
description: Run the full auto-pipeline — search my profile's platforms, fit-score new roles, select the top-N, auto-tailor each, readiness-score each, and present a ranked review-ready shortlist; I pick which to apply to and every apply still goes through assisted /job-apply.
---

Run the `job-pipeline` skill. Search my profile's platforms, fit-score the new
roles, auto-tailor the top few (default 5, or the number I give), readiness-score
each, and show me a ranked shortlist of company/role · fit · ATS · readiness · top
gaps · pack path. I pick which to apply to; every apply still goes through
`/job-apply` and I approve every send — nothing is auto-submitted. Never fabricate
to raise a score; surface any pack that fails the fabrication gate as blocked,
route genuine gaps to `/job-upskill`, and skip any platform whose tool isn't
available with a note rather than faking it.
